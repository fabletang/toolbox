By Fable Tang
---------------------------
*Test.java 为单元测试代码,也是很好的使用例子,*Test.java依赖于junit4 jar包，如果 junit4与android 冲突，编译设置的时候把junit4 jar放在最前面即可

ByteStringHex 字节字符串16进制处理类
TagJsonUtils  解释tag.json
TLVUtils      TLV 与 byte[] 的转换类

tag.json   tag解释字典， 未完整，可以持续补充。可以位于jar包，也可以放在普通目录，只要跟TagjsonUtils在一起既可。 但是普通目录优先
---------------------------
